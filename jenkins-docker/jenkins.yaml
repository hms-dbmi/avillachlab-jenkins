credentials:
  system:
    domainCredentials:
      - credentials:
         - usernamePassword:
            id: gitlab_token
            username: ${GITLAB_USER}
            password: ${GITLAB_TOKEN}
