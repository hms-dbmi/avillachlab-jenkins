<?xml version='1.1' encoding='UTF-8'?>
<project>
  <actions/>
  <description>Downloads one stack&apos;s bdc.env and the shared configuration.json into configs/, for editing and re-upload through Upload PIC-SURE-Frontend Configuration. bdc.env is per stack, so the choice matters; configuration.json is the same file for every stack.</description>
  <keepDependencies>false</keepDependencies>
  <properties>
    <hudson.model.ParametersDefinitionProperty>
      <parameterDefinitions>
        <hudson.model.ChoiceParameterDefinition>
          <name>TARGET_STACK</name>
          <description>Which stack&apos;s bdc.env to act on. Based on selection we determine if is a or b stack. configuration.json is shared by both stacks and is not affected by this choice.</description>
          <choices>
            <string>staging</string>
            <string>live</string>
          </choices>
        </hudson.model.ChoiceParameterDefinition>
      </parameterDefinitions>
    </hudson.model.ParametersDefinitionProperty>
  </properties>
  <scm class="hudson.scm.NullSCM"/>
  <canRoam>true</canRoam>
  <disabled>false</disabled>
  <blockBuildWhenDownstreamBuilding>false</blockBuildWhenDownstreamBuilding>
  <blockBuildWhenUpstreamBuilding>false</blockBuildWhenUpstreamBuilding>
  <jdk>(System)</jdk>
  <triggers/>
  <concurrentBuild>false</concurrentBuild>
  <builders>
    <hudson.tasks.Shell>
      <command>#!/bin/bash

set -e
# Source folder containing the scripts
source_scripts_folder=&quot;${JENKINS_HOME}/workspace/Bash_Functions/&quot;
ls -la &quot;$source_scripts_folder&quot;

# Iterate through the files in the folder and source them
for script_file in &quot;$source_scripts_folder&quot;*.sh; do
    chmod +x &quot;$script_file&quot;
    if [ -f &quot;$script_file&quot; ] &amp;&amp; [ -x &quot;$script_file&quot; ]; then
        echo &quot;sourcing $script_file&quot;
        source &quot;$script_file&quot;
    fi
done


: &quot;${TARGET_STACK:?TARGET_STACK is required}&quot;
target_stack=$(get_stack &quot;$TARGET_STACK&quot;)
echo &quot;Resolved TARGET_STACK ${TARGET_STACK} to stack ${target_stack}&quot;

mkdir -p configs
aws s3 --sse=AES256 cp &quot;s3://$stack_s3_bucket/configs/pic-sure-frontend/$target_stack/bdc.env&quot; configs/bdc.env
aws s3 --sse=AES256 cp &quot;s3://$stack_s3_bucket/configs/pic-sure-frontend/configuration.json&quot; configs/configuration.json
</command>
      <configuredLocalRules/>
    </hudson.tasks.Shell>
  </builders>
  <publishers>
    <hudson.tasks.ArtifactArchiver>
      <artifacts>configs/*.env, configs/*.json</artifacts>
      <allowEmptyArchive>false</allowEmptyArchive>
      <onlyIfSuccessful>false</onlyIfSuccessful>
      <fingerprint>false</fingerprint>
      <defaultExcludes>true</defaultExcludes>
      <caseSensitive>true</caseSensitive>
      <followSymlinks>false</followSymlinks>
    </hudson.tasks.ArtifactArchiver>
  </publishers>
  <buildWrappers>
    <hudson.plugins.ws__cleanup.PreBuildCleanup plugin="ws-cleanup@0.46">
      <deleteDirs>false</deleteDirs>
      <cleanupParameter></cleanupParameter>
      <externalDelete></externalDelete>
      <disableDeferredWipeout>false</disableDeferredWipeout>
    </hudson.plugins.ws__cleanup.PreBuildCleanup>
  </buildWrappers>
</project>